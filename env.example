# Copy to `.env.local` (recommended) or set env vars in your host.
# Never commit real secrets (database URLs, passwords, API keys).
TIMESCALE_SERVICE_URL=

# Backend auth (Netlify Functions)
# Generate a long random string and set it in your host environment.
JWT_SECRET=

# One-time DB initialization endpoint protection:
# Set a random string and call POST /api/init-db with header X-Admin-Token.
ADMIN_INIT_TOKEN=

# Sightengine moderation (required for signup: username + avatar SFW checks)
SIGHTENGINE_USER=
SIGHTENGINE_SECRET=

